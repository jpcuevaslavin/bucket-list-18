<h3>
  Usuarios que han completado la tarea <%= @todo.title %>:
</h3>

<ol>
  <% @todo.users.each do |user| %>
    <li><%= user.email %></li>
  <% end %>
</ol>

<%= link_to 'Volver al inicio', root_path, class: 'btn btn-success' %>
